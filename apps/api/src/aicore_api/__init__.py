"""AICore API — Phase 0 foundation.

Only the health surface is implemented. Domain modules (identity, policy,
inventory, audit, …) are deliberately absent; they belong to later phases.
"""

__version__ = "0.1.0"
